import { Placeholder } from './_page';

export default function GuideView() {
  return (
    <Placeholder
      title="Guide"
      phase={5}
      what="Rebuilt as deep-linkable tabbed sections (#/guide/<section>), including the Mobile setup section."
    />
  );
}
