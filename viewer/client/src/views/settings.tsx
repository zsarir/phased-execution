import { Placeholder } from './_page';

export default function SettingsView() {
  return (
    <Placeholder
      title="Settings"
      phase={5}
      what="Notifications, permission rules and devices. The theme switcher already lives in the shell — the rail's footer on a desktop, the More sheet on a phone."
    />
  );
}
