import { Placeholder } from './_page';

export default function NotificationsView() {
  return (
    <Placeholder
      title="Notifications"
      phase={5}
      what="Everything the console has announced, whether or not anything was listening at the time."
    />
  );
}
