import { Placeholder } from './_page';

export default function SearchView() {
  return (
    <Placeholder
      title="Search"
      phase={5}
      what="Full-text across every plan, handoff and phase at once."
    />
  );
}
