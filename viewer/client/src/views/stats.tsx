import { Placeholder } from './_page';

export default function StatsView() {
  return (
    <Placeholder
      title="Statistics"
      phase={5}
      what="Throughput, cost and the shape of the portfolio, on the four hand-written SVG chart types ported from the legacy client."
    />
  );
}
