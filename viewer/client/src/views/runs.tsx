import { Placeholder } from './_page';

export default function RunsView() {
  return (
    <Placeholder
      title="Runs"
      phase={4}
      what="The run view — live phases, the transcript, approvals, the /btw box and the one StatusStack that replaces nine banners — is rebuilt in Phase 4."
    />
  );
}
